# Warm Paws 云端配置模板
# 复制本文件为 .env 后填入真实值（.env 已被 .gitignore 忽略，绝不会进仓库）
# 说明：anon/publishable key 是公开密钥，安全由 Supabase RLS 策略保证
#
# 云端部署（Cloudflare Workers / Pages）时，请在后台环境变量里配置同样两个键名：
#   VITE_SUPABASE_URL
#   VITE_SUPABASE_ANON_KEY

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# ── 阶段 2：API 网关模式（可选，默认关）────────────────────────
# 置 1 时前端数据请求走同源 /api/*（由 Cloudflare Worker 翻译到 Supabase REST/Storage），
# Auth 仍直连 Supabase。启用前需要先部署 Worker 并在 Worker 侧配置两个变量（anon key 是公开密钥）：
#   npx wrangler secret put SUPABASE_URL
#   npx wrangler secret put SUPABASE_ANON_KEY
# 本地联调：把同样的两个键写进 .dev.vars（已被 .gitignore 忽略）→ npm run dev:api
#           再以 VITE_API_GATEWAY=1 启动前端（vite 已把 /api 代理到 127.0.0.1:8787）
# VITE_API_GATEWAY=0